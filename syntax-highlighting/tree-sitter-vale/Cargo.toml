[package]
name = "tree-sitter-vale"
version = "0.1.0"
edition = "2021"
build = "build.rs"
description = "Vale language grammar for tree-sitter"
license = "MIT"

[lib]
path = "src/lib.rs"

[dependencies]
tree-sitter-language = "0.1"
tree-sitter = "0.24"

[build-dependencies]
cc = "1"
