// expected_exit: 0
// summary: A second std type — rust.std.string.String. String::new().len() is 0.
// Exercises ValeRuster on String (non-generic) vs Vec (generic).

import rust.std.string.String;

exported func main() int {
  s = String.new();
  return s.len().TruncateI64ToI32();
}
