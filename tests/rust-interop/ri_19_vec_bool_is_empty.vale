// expected_exit: 1
// summary: Combine the Bug 1 fix (bool return) with the Bug 2 fix (bool generic) —
// Vec<bool>.is_empty() exercises both paths in one call.

import rust.std.vec.Vec;

exported func main() int {
  v = Vec<bool>.new();
  if (v.is_empty()) {
    return 1;
  }
  return 0;
}
